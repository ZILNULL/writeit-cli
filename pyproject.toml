[project]
name = "writeit-cli"
version = "0.4.0"
requires-python = ">=3.14.5"
dependencies = ["textual[syntax]>=8.2.7", "textual-dev>=1.8.0"]

[tool.pyright]
include = ["src", "tests"]

[dependency-groups]
dev = ["pytest>=9.0.3", "pytest-asyncio>=1.3.0"]
